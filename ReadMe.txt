Phase1:
1.Shortens URL
2.Keeps track of total visits

POST/URL -> GENERATES AND RETURS SHORTENED URL

GET/:id -> REDIRECTS TO THE ORIGINAL URL

GET/URL/analytics/:id -> RETURS CLICKS ON THE PROVIDED SHORT ID

Phase2: 
Added the view home and enables the server side rendering through EJS
GET(/home)

Phase3:
Adding the sessions for login(statefull) and displaying only user's content --in progress--

Phase 4:
JWT

TO-DO(future):
1 Deploy it
2.Gemini chatbot integration and re-deploy
